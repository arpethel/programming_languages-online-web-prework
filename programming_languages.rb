require "pry"

def reformat_languages(languages)
  def reformat_languages(languages)
  new_hash_v = {}
  languages.each do |k, v|
    v.each do |lang|
      new_hash_v[lang][v] = {}
      binding.pry
    end


  end

end
end

# puts reformat_languages(languages)
